<% if current_user.attending?(study_session) %>
  <%= button_to "Cannot Attend", attendee_path(current_user.attendance_record(study_session)), :method => 'delete' %>
<% else %>
  <%= button_to "Attend", attendees_path(study_session_id: study_session) %>
<% end %>
