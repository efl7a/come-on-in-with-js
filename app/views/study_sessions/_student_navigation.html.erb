<% if current_user.attending?(session) %>
  <%= button_to "Cannot Attend", attendee_path(current_user.attendance_record(session)), :method => 'delete' %> 
<% else %>
  <%= button_to "Attend", attendees_path(session_id: session) %>
<% end %>
<br><br>
